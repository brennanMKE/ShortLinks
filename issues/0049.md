# 0049 — Add visualizations (charts) for short-link click metrics

| | |
|---|---|
| **Status** | resolved |
| **Module** | web / clicks |
| **Platform** | All |
| **First seen** | 2026-06-18 |
| **Closed** | 2026-06-18 |
| **Commit** | 3ababc2 |

## Description

Improve the short-link metrics display with actual visualizations — a clicks-over-time chart and visual breakdowns of the UTM dimensions — instead of today's text-only sorted lists. `LinkDetail.svelte` currently renders total clicks and per-UTM counts as plain lists; the backend stats in `internal/clicks/stats.go` already provide the data to chart.

## Motivation

The data to make metrics legible at a glance already exists but is presented as text. A time-series of clicks and proportional breakdowns (by source/medium/campaign) make trends and top performers obvious in a way ranked lists do not.

## Proposed approach

- Add a clicks-over-time series (line or bar) and proportional breakdowns (bar/donut) for the UTM dimensions on `LinkDetail`.
- **Charting library decision is part of this issue.** The SPA is embedded in the Go binary, so bundle size matters — prefer a lightweight option (e.g. hand-rolled inline SVG for these simple charts, or a small dependency) over a heavyweight suite. Document the choice and its gzipped cost; verify the production build size impact.
- Confirm whether the stats API exposes (or needs to expose) a time bucket for clicks-over-time — `internal/clicks/stats.go` may need a new query/endpoint for time-bucketed counts. Flag any backend change clearly; if it grows large, carve it into a `clicks`-module follow-up issue.
- Keep any data-shaping logic as pure, testable functions in `web/src/lib/`.
- Ensure charts are accessible (text alternative / data still available to screen readers) and render sensibly with empty or single-point data.

## Acceptance criteria

- [x] `LinkDetail` shows a clicks-over-time chart and visual UTM breakdowns
- [x] Charting approach chosen, justified, and its bundle-size impact recorded
- [x] Any new backend stats endpoint (if needed) is implemented and tested in `internal/clicks`
- [x] Empty-stats and single-data-point cases render gracefully (no broken axes)
- [x] Charts have an accessible fallback
- [x] `cd web && npm run build` and `npm run check` clean; Go tests pass if backend touched

## Relation

- Related: [#0048](0048.md) (UTM builder — produces the dimensions visualized here)
- Related: [#0050](0050.md) / [#0051](0051.md) (charts must be responsive and theme-aware)

## Implementation

Added click-metrics visualizations to `LinkDetail`. **Backend:** new `StatsStore.ClicksOverTime` in `internal/clicks/stats.go` — groups clicks by UTC calendar day (`date_trunc('day', clicked_at AT TIME ZONE 'UTC')`) over a half-open `[from, to)` window defaulting to the last 30 days (`today-30 … today-1`, excluding the incomplete current day), parameterized (no injection); wired through the link-stats handler (`internal/handlers/links.go`, `statsProvider` interface + `linkDetailView.Timeseries`, kept optional behind the existing `h.stats != nil` guard) and `web/src/lib/api.ts`/`types.ts`. **Frontend:** hand-rolled inline-SVG charts (no charting dependency) — `ClicksChart` (clicks-over-time area+line) and `UTMBarChart` (proportional UTM breakdowns) — with pure data-shaping helpers in `web/src/lib/charts.ts`. All colors come from #0052 design tokens (dark-ready), SVG uses `viewBox` + `width:100%` (responsive-ready). Built on `issue/0049` (Sonnet implement, Opus review → APPROVE), squash-merged to `main` as `3ababc2`.

## Verification

- **Web** (from `web/`): `npm run check` → 181 files, 0 errors; `npm run build` → clean (JS 124.4 kB / gzip 41.4 kB, CSS 16.7 kB / gzip 3.34 kB; **+3.07 kB gzip**, no charting lib); `npm test` → 162 tests passing incl. **26 new** `charts.test.ts` (gap-fill across month/year boundaries, single-point centering, all-zero NaN guard, proportion math).
- **Go**: `go build ./...` clean; **DB tests run for real** against local `shortlinks_test` — `TEST_DATABASE_URL=postgres://localhost:5432/shortlinks_test?sslmode=disable go test ./internal/clicks/...` → `TestClicksOverTime_BasicBuckets / _NoClicks / _ZeroDefaults` all PASS; handler tests pass. The reviewer independently re-ran the DB tests and confirmed PASS (not skipped).

## Files changed

- `internal/clicks/stats.go` — `DayBucket`, `TimeseriesResult`, `ClicksOverTime` query.
- `internal/clicks/stats_test.go` — 3 new DB tests.
- `internal/handlers/links.go` — `statsProvider.ClicksOverTime`, `linkDetailView.Timeseries`, handler fetch.
- `web/src/lib/types.ts` — `DayBucket`/`TimeseriesResult` + `LinkDetail.timeseries`.
- `web/src/lib/charts.ts` — NEW pure chart helpers; `charts.test.ts` — NEW 26 tests.
- `web/src/lib/ClicksChart.svelte`, `web/src/lib/UTMBarChart.svelte` — NEW inline-SVG chart components.
- `web/src/views/LinkDetail.svelte` — render the charts (totals + UTM data preserved).

## Gotchas

- Clicks-over-time deliberately **excludes the current (incomplete) UTC day**; backend window and frontend `defaultDateRange(30)` are both `[today-30 … today-1]` — checked for off-by-one at both edges, none.
- Charts are token-only with no dark override of their own; when #0051 lands they theme automatically. SVG is `viewBox`-scaled so #0050 gets responsive charts for free.
- Non-blocking follow-ups (reviewer): `stats_test.go` plants clicks mid-day so it doesn't exercise the near-midnight non-UTC-session timezone edge (SQL is the correct idiom; add a discriminating test later); a stale "timeseriesDays" doc comment near `zeroTime` in `links.go`; dead `rec := NewRecorder` scaffolding in `stats_test.go`.

## Work log

| Date | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|
| 2026-06-18 | claude-sonnet-4-6 | 1,894 | 26,229 | 3,882,884 | 90,035 | $1.90 |
| 2026-06-18 | claude-opus-4-8 | 3,632 | 11,036 | 1,249,235 | 56,683 | $1.27 |

**Total: $3.17**
