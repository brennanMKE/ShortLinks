# 0001 — Project scaffolding — Go module, directory layout, .env.example, .gitignore

| | |
|---|---|
| **Status** | open |
| **Module** | config / infra |
| **Platform** | All |
| **First seen** | 2026-05-25 |

## Description

Set up the initial Go project structure exactly as specified in the PRD. This is the foundation all other issues build on and must be completed first.

## Acceptance Criteria

- [ ] `go.mod` initialised (`module github.com/brennanMKE/ShortLinks`)
- [ ] Directory tree created: `cmd/shortlinks/`, `internal/config/`, `internal/db/`, `internal/cache/`, `internal/auth/`, `internal/links/`, `internal/clicks/`, `internal/filters/`, `internal/audit/`, `internal/events/`, `internal/handlers/`, `internal/middleware/`, `migrations/`, `web/`
- [ ] `.env.example` created with all variables from the PRD Configuration section (PORT, BASE_URL, DATABASE_URL, WEBAUTHN_RP_ID, WEBAUTHN_RP_ORIGIN, SESSION_SECRET, SES_SMTP_*, EMAIL_FROM, CACHE_MAX_COST, CACHE_TTL_SECONDS, ADMIN_EMAIL)
- [ ] `.gitignore` excludes `.env`, `web/dist/`, Go binaries, and OS junk files
- [ ] `cmd/shortlinks/main.go` compiles with a minimal `main()` that prints version and exits
- [ ] `go build ./...` passes with no errors

## Notes

PRD reference: Go Service Architecture section.
The `web/` Svelte project is scaffolded in issue #0033 — leave the folder empty here.
