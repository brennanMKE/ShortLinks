# 0041 — Magic-link verify URLs return raw JSON instead of an enrollment page

| | |
|---|---|
| **Status** | closed |
| **Module** | handlers / web / auth |
| **Platform** | All |
| **First seen** | 2026-06-17 |
| **Closed** | 2026-06-17 |
| **Branch** | issue/0041 |

## Description

The registration and recovery emails link to `{BASE_URL}/auth/register/verify?token=…` and `{BASE_URL}/auth/recover/verify?token=…` (`internal/auth/mailer.go:46,51`), but those paths are registered as JSON API handlers (`main.go:178,186`) that return WebAuthn `PublicKeyCredentialCreationOptions`. A browser following the link from an email therefore renders a raw JSON blob — there is no HTML/JS to read the token and run the passkey ceremony. This blocks every email-driven enrollment flow.

## Long Description

The SPA is store-driven with no client-side router and no code that reads a `?token=` query param (confirmed: nothing in `web/src` references `location.search`, `searchParams`, `register/verify`, or `recover/verify`). The catch-all SPA handler is served last (`main.go:244`), but the explicit `GET /auth/*/verify` routes shadow it, so even a deep-link fallback never reaches `index.html`.

A coherent fix needs a decision on the URL scheme. Two viable approaches:
1. Point the email links at an SPA-served path (e.g. `/register?token=…`, `/recover?token=…`); the SPA reads the token on load and calls a JSON endpoint to fetch the creation options.
2. Keep the email link on `/auth/*/verify` but have that GET serve the SPA shell (HTML), and move the JSON options to a separate path the SPA fetches.

This is the shared foundation for #0042 (registration completion UI) and #0043 (recovery UI).

## Expected behavior

Clicking a magic link in an email lands on a page that loads the SPA, reads the token, and begins the passkey ceremony.

## Actual behavior

The browser displays raw JSON WebAuthn options; nothing runs.

## Acceptance Criteria

- [x] Decide and document the magic-link URL scheme (SPA route vs. HTML-serving verify endpoint)
- [x] Email links produced by `verificationURL`/`recoveryURL` resolve to a browser page that loads the SPA, not a JSON response
- [x] The SPA reads the `token` from the landing URL and makes it available to the registration/recovery ceremony
- [x] The JSON creation-options payload is reachable by the SPA via a documented endpoint (renamed/relocated if needed) without colliding with the browser-facing link
- [x] A hard refresh on the landing URL still works (SPA fallback to `index.html`)
- [x] `go build ./...` and `cd web && npm run build` pass

## Relation

- Pairs with: [#0042](0042.md) (registration completion UI), [#0043](0043.md) (recovery UI)

## Resolution notes

> 🟢 Resolved 2026-06-17 — Email magic-links now point at non-`/auth` SPA paths (`/register/verify`, `/recover/verify`) that fall through to the catch-all and load the SPA, which parses the token and routes to a landing view. JSON options endpoints stay at `/auth/*/verify`. Ceremony itself is deferred to #0042/#0043.

## Root cause

`verificationURL`/`recoveryURL` built links to `/auth/register/verify` and `/auth/recover/verify`, which are registered JSON API handlers — a browser following an emailed link rendered raw JSON, and the SPA had no route, token parsing, or view for an enrollment landing.

## Fix

Changed the two mailer URL builders to emit `{BASE_URL}/register/verify?token=…` and `/recover/verify?token=…` (no `/auth` prefix, so they fall through to the `GET /` SPA catch-all). Added a `pendingVerifyToken` store and two `View` values (`register-verify`, `recover-verify`); `App.svelte` `onMount` detects the landing paths, parses `?token=`, stashes it, and routes to the view without calling `/api/me`. Added `registerVerify`/`recoverVerify` API helpers and two scaffold views that fetch the JSON creation options to prove the round-trip, with TODO seams for the `navigator.credentials.create()` ceremony (#0042/#0043).

## Review

Reviewed by `claude-opus-4-8`, one round, approved first pass. Independently re-ran `go build ./...`, `go test ./internal/auth/...`, `npm run build`, and `svelte-check`; confirmed the new paths aren't shadowed by mux routes, the mailer URL tests assert the new paths, and the `internal/config` test failure is pre-existing (untouched by this diff). Non-blocking nits (idiomatic `$store` access; `creationOptions` held-but-unused until #0042/#0043) recorded for the follow-ons.

## Verification

- `go build ./...` — clean.
- `go test ./internal/auth/...` — PASS; mailer tests assert the new `/register/verify` and `/recover/verify` URLs.
- `go test ./...` — only pre-existing `internal/config` `TestLoad_DefaultsApplied` fails (missing `BASE_URL`; unrelated, no config files in diff).
- `cd web && npm run build` — built (128 modules); `svelte-check` 0 errors covering the two new views.

## Files changed

- `internal/auth/mailer.go` — `verificationURL`/`recoveryURL` emit SPA paths; doc comments explain the scheme.
- `internal/auth/ses_mailer_test.go` — assertions updated to the new SPA paths.
- `web/src/lib/stores.ts` — `View` union gains `register-verify`/`recover-verify`; new `pendingVerifyToken` store.
- `web/src/lib/api.ts` — `registerVerify`/`recoverVerify` GET helpers (with #0042/#0043 TODOs for finish wrappers).
- `web/src/App.svelte` — landing-path detection, token parse, view routing on mount.
- `web/src/views/RegisterVerify.svelte`, `web/src/views/RecoverVerify.svelte` — new scaffold views (verifying/ready/error), ceremony deferred.

## Gotchas

`web/dist/*` is gitignored except the placeholder `index.html`; do NOT commit a real build — the embed bundle is regenerated at deploy. A local `npm run build` will dirty `dist/index.html`; discard it rather than committing.

## Work log

| Date | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|
| 2026-06-17 | claude-sonnet-4-6 | 3,992 | 8,349 | 1,316,061 | 52,407 | $0.73 |
| 2026-06-17 | claude-opus-4-8 | 6,260 | 4,199 | 454,557 | 33,075 | $0.57 |

**Total: $1.30**
