# 0056 — Umbrella: Local dev runtime to build & run Short Links on macOS (no systemd, no Postgres) with iPhone Simulator testing

| | |
|---|---|
| **Status** | resolved |
| **Module** | infra / db / auth / web |
| **Platform** | macOS |
| **First seen** | 2026-06-22 |
| **Closed** | 2026-06-24 |

## Description

Make it possible to build and run the Short Links website directly on this Mac for fast local iteration and **visual verification** — without provisioning PostgreSQL or systemd, and with a mock signed-in account so `localhost` reaches the dashboard. With a local server running, the iOS Simulator's Safari can load it to verify responsive layout and JS / native-control behavior. This umbrella tracks the sub-issues that together deliver that loop.

## Motivation

Several recent UI changes (#0050 responsive, #0051 dark mode, and especially #0053 — the Expires field width/height) could **not** be visually verified in this environment, which forced repeated deploy-and-guess cycles against production. A local build → run → inspect loop, including real mobile Safari via the iOS Simulator, closes that gap and would have caught the #0053 rendering issues immediately.

## Sub-issues

- [x] [#0057](0057.md) — Storage abstraction + JSON-file dev backend (replace Postgres for dev)
- [x] [#0058](0058.md) — Dev-only auth bypass with a mock admin user (skip the passkey ceremony on localhost)
- [x] [#0059](0059.md) — One-command local run (Makefile/script) without systemd or Postgres
- [x] [#0060](0060.md) — iPhone Simulator (iOS Safari) verification workflow for layout & JS

## Acceptance criteria (umbrella)

- [x] A single command builds and serves the app on `localhost` with no Postgres, no systemd, no migrations
- [x] Opening `localhost` lands on the dashboard as a mock admin (no real passkey)
- [x] App data persists to JSON files (or in-memory) in dev; production stays Postgres-backed and unchanged
- [x] The iOS Simulator's Safari can load the local URL and verify responsive layout / native-control rendering (e.g. the #0053 datetime field)

## Guardrails

Dev-only: the JSON backend and the auth bypass MUST be impossible to enable in production — guarded by explicit dev config and/or a build tag, and refused when `BASE_URL`/RP id is not localhost. PostgreSQL + WebAuthn passkeys remain the sole production path.

## Relation

- Directly motivated by [#0053](0053.md) (couldn't verify iOS rendering), [#0050](0050.md), [#0051](0051.md).

## Resolution

All four sub-issues landed (#0057 `830c712`, #0058 `068efe4`, #0059 `9986c8e`, #0060 `5bfd550`). The local dev runtime now works end to end:

- **#0057** — `internal/devstore`, an in-memory store satisfying the existing handler interfaces, selected by an explicit `STORAGE=json` (never an empty `DATABASE_URL`, so prod can't engage it). The app boots and serves with no Postgres.
- **#0058** — `DevAutoLogin` middleware (dev-only, fail-closed panic guardrail) auto-sessions the seeded mock admin, so `localhost` lands on the dashboard with no passkey.
- **#0059** — `scripts/dev.sh` (+ `Makefile`): one command runs the Go API (`STORAGE=json`) + Vite on localhost, no systemd/Postgres/migrations. `docs/dev.md` documents it.
- **#0060** — `scripts/sim.sh`: boot an iOS Simulator, load the local app, screenshot — verifying mobile Safari rendering locally. Confirmed by `issues/0060/dashboard.png`, which shows the dashboard rendered in an iPhone 17 sim with the Expires field matching the other inputs.

**Net effect:** `./scripts/dev.sh` (desktop) and `./scripts/sim.sh` (iOS Simulator) replace deploy-and-guess for UI verification — the gap that caused the #0053/#0061/#0076 regressions. Production (Postgres + WebAuthn + systemd + `scripts/deploy.sh`) is provably untouched throughout; every dev affordance is gated behind `STORAGE=json`.
