# 0054 — Replace the hardcoded "go.sstools.co" branding in the web UI with the product name "Short Links"

| | |
|---|---|
| **Status** | resolved |
| **Module** | web |
| **Platform** | All |
| **First seen** | 2026-06-22 |
| **Closed** | 2026-06-22 |
| **Commit** | 98e9e55 |

## Description

The web UI displays the deployment domain `go.sstools.co` as the app's name/title — the header brand on Dashboard/Account/Admin and the login/verify page headings. ShortLinks is a self-hostable project that anyone can deploy under their own domain, so the UI should show the product name **"Short Links"** instead of one specific instance's hostname.

## Motivation

`go.sstools.co` is *this* deployment's domain, not the product. Baking it into the shipped UI means a fresh deployment under a different domain would still read "go.sstools.co" (or surface its own hostname where a product name belongs). Using the static brand "Short Links" makes the UI correct for every deployment.

## Proposed approach

- Replace the displayed app title/brand with the literal "Short Links" everywhere the domain is currently shown *as the name*: the header `app-title` (`App`/`Dashboard`/`Account`/`Admin`) and the `Login` / `RegisterVerify` / `RecoverVerify` headings.
- Leave functional URLs untouched — the domain still legitimately appears in generated short URLs and the WebAuthn RP origin; only the **branding/name** usage changes.
- Find where the title string originates (likely `BASE_URL` or the RP ID rendered as a heading) and swap it for a static product name; consider a single shared constant so the brand is defined once.

## Acceptance criteria

- [x] The header brand and auth-page headings read "Short Links", not the deployment domain
- [x] No functional URLs (generated short links, RP origin/ID) are changed
- [x] `cd web && npm run build` and `npm run check` clean

## Relation

- Touches the same SPA views as [#0052](0052.md) (design system).

## Implementation

The brand shown in the header (`.app-title` on Dashboard/Account/Admin) and the headings on Login/RegisterVerify/RecoverVerify were **hardcoded** to the literal `go.sstools.co` in each view — one deployment's domain. Centralized the brand into a new `web/src/lib/branding.ts` (`export const APP_NAME = 'Short Links'`), imported it into all six views, and set `web/index.html`'s `<title>` to "Short Links". Functional values were deliberately left untouched — `SHORT_URL_BASE` (`web/src/lib/links.ts`) and the WebAuthn `rp.id`/`rp.name` (`web/src/lib/webauthn.ts`) are not in the diff, so generated short URLs and the passkey RP are unchanged. Built on `issue/0054` (Sonnet implement, Opus review → APPROVE), squash-merged to `main` as `98e9e55`.

## Verification

`cd web && npm run check` → 182 files, 0 errors; `npm run build` → clean; `npm test` → 162 passing (unchanged). Reviewer independently confirmed `links.ts`/`webauthn.ts` are absent from `git diff main...issue/0054 --name-only`, `SHORT_URL_BASE` intact at `links.ts:19`, the WebAuthn `rp` path intact, and `git grep go.sstools.co` over the views returns nothing.

## Files changed

- `web/src/lib/branding.ts` — NEW: single source of truth, `APP_NAME = 'Short Links'`.
- `web/index.html` — `<title>` → "Short Links".
- `web/src/views/{Login,Dashboard,Account,Admin,RegisterVerify,RecoverVerify}.svelte` — render `{APP_NAME}` instead of the hardcoded domain.

## Gotchas

- Displayed brand is "Short Links" (two words) per this issue; the repo / `package.json` / WebAuthn `rp.name` keep the one-word "ShortLinks". Intentional — only the human-facing brand changed.
- The domain still legitimately appears in `SHORT_URL_BASE` and short-link doc comments; those are functional, not branding, and were left alone.

## Work log

| Date | Model | Input | Output | Cache read | Cache write | Cost |
|---|---|---|---|---|---|---|
| 2026-06-22 | claude-sonnet-4-6 | 38 | 6,167 | 2,093,516 | 71,519 | $0.99 |
| 2026-06-22 | claude-opus-4-8 | 3,635 | 3,760 | 221,357 | 31,014 | $0.42 |

**Total: $1.41**
