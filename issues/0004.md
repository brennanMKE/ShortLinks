# 0004 — Deployment instructions document

| | |
|---|---|
| **Status** | resolved |
| **Module** | infra |
| **Platform** | All |
| **First seen** | 2026-05-25 |
| **Closed** | 2026-05-25 |
| **Commit** | dd14062 |

## Description

Write a `DEPLOYMENT.md` at the project root covering the full end-to-end process to deploy ShortLinks on a fresh EC2 instance. This document is used by the initial operator and by anyone cloning the repo to run their own server.

## Acceptance Criteria

- [ ] `DEPLOYMENT.md` covers all of the following sections in order:
  1. **Prerequisites** — EC2 instance (Ubuntu 22.04+), Apache 2 installed, PostgreSQL installed, Node.js 20+ and Go 1.22+ installed, DNS wildcard pointing to EC2
  2. **Database setup** — run `scripts/db/create.sql`, configure password
  3. **Configuration** — copy `.env.example` to `/etc/shortlinks/config.env`, fill in all values (with notes on generating `SESSION_SECRET` via `openssl rand -hex 32`)
  4. **Build** — `cd web && npm ci && npm run build`, then `go build -o shortlinks ./cmd/shortlinks`
  5. **Migrations** — `migrate -path migrations -database $DATABASE_URL up`
  6. **Seed** — run seed command to create admin user and test link
  7. **systemd** — install unit file, enable and start service
  8. **Apache** — install virtual host config, enable site, reload Apache
  9. **TLS** — obtain Let's Encrypt certificate with Certbot
  10. **First login** — register account at `https://go.sstools.co` using the `ADMIN_EMAIL` address
- [ ] Each step includes the exact commands to run
- [ ] A **Updating** section covers the redeploy flow (rebuild, restart service, run new migrations)

## Notes

PRD reference: Deployment section. Cross-reference issues #0002, #0003, #0011, #0012.

## Implementation

Created `DEPLOYMENT.md` at the repo root. It covers all ten required sections in
the exact order specified by the acceptance criteria — Prerequisites, Database
setup, Configuration, Build, Migrations, Seed, systemd, Apache, TLS, First
login — followed by an **Updating (redeploy)** section. Each section provides
concrete, copy-pasteable commands.

Content is anchored to the artifacts already in the repo so the doc is
self-consistent:

- DB setup runs `scripts/db/create.sql` (and `scripts/db/drop.sql` for resets),
  using the `shortlinks` role/database and noting the `CHANGE_ME_IN_PRODUCTION`
  password placeholder.
- Configuration copies `.env.example` to `/etc/shortlinks/config.env` and
  documents every variable defined there, including generating `SESSION_SECRET`
  with `openssl rand -hex 32`.
- Build is `cd web && npm ci && npm run build` then
  `go build -o shortlinks ./cmd/shortlinks`.
- Migrations use `migrate -path migrations -database "$DATABASE_URL" up`.
- Seed runs `go run ./cmd/shortlinks seed`.
- Apache installs `deploy/apache/go.sstools.co.conf` via `a2ensite`; TLS uses
  `certbot --apache -d go.sstools.co`.
- The systemd unit is referenced at `deploy/systemd/shortlinks.service` and
  forward-referenced to issue #0012 (the unit file is not created yet).

## Verification

This is a documentation deliverable, verified by inspection against the repo's
actual files. Confirmed all ten sections appear in the required order plus the
Updating section, and that every cited artifact path exists in the repo
(`scripts/db/create.sql`, `scripts/db/drop.sql`, `.env.example`,
`deploy/apache/go.sstools.co.conf`, `cmd/shortlinks`, `migrations/`) — except
`deploy/systemd/shortlinks.service`, which is intentionally forward-referenced to
issue #0012. The configuration table matches `.env.example` exactly. Ran
`go build ./...` (exit 0) to confirm the Go build is unaffected.

## Files changed

- `DEPLOYMENT.md` — new file at repo root; full EC2 deployment guide plus
  redeploy/Updating flow.

## Gotchas

- `migrations/` and `web/` are currently placeholder directories (only
  `.gitkeep`), since the Phase 1+ build work has not landed. The doc references
  the intended commands (`migrate ... up`, `npm run build`) per the PRD and the
  issue's acceptance criteria, which is correct — those commands will operate on
  real files once the corresponding issues are implemented.
- The `deploy/systemd/shortlinks.service` path is cited as a forward reference;
  do not treat its absence as a broken link — it is owned by issue #0012.
