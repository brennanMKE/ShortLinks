# 0065 — Document the database schema & migrations (docs/database.md)

| | |
|---|---|
| **Status** | resolved |
| **Module** | db |
| **First seen** | 2026-06-22 |
| **Closed** | 2026-06-23 |
| **Commit** | 30d7e36 |

## Description

Write `docs/database.md` documenting the PostgreSQL schema and the migration/seed workflow.

## Scope

- Each table from `migrations/`: `users`, `links`, `clicks`, `sessions`, `settings`, `passkey_credentials`, `audit_log`, `url_filter_rules`, `pending_registrations` — purpose, key columns, and relationships.
- The migration workflow: `golang-migrate`, the `NNNN_*.{up,down}.sql` convention, applying with `migrate ... up`, and how to add a migration. Cross-link `DEPLOYMENT.md` / the deploy doc's "is a migration needed?" note.
- The `seed` command (admin user from `ADMIN_EMAIL` + a test link) and idempotency.

## Acceptance criteria

- [ ] `docs/database.md` covers every table in `migrations/` and the migrate + seed workflow
- [ ] Accurate to the actual migration files

## Relation

- Umbrella: [#0062](0062.md).

## Resolution

Written in [`docs/database.md`](../docs/database.md) as part of the #0062 documentation set (commit `30d7e36`), grounded in the cited source files. First pass — review in the Mac app and flag any inaccuracies for a follow-up edit.
