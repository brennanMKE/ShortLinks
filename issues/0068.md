# 0068 — Document links, redirects, key generation & caching (docs/links.md)

| | |
|---|---|
| **Status** | open |
| **Module** | links / cache |
| **First seen** | 2026-06-22 |

## Description

Write `docs/links.md` documenting the core link domain and the redirect hot path.

## Scope

- Link creation: short-key generation, deduplication, custom aliases, and expiry (`expires_at`).
- The redirect path `GET /u/{key}`: lookup, expiry handling, and the response.
- The Ristretto redirect cache: what's cached, hit/miss flow, invalidation on change, and `CACHE_MAX_COST` / `CACHE_TTL_SECONDS` tuning.
- `SHORT_URL_BASE` / how the displayed short URL is composed (`web/src/lib/links.ts`).

## Acceptance criteria

- [ ] `docs/links.md` covers key generation/dedup/alias/expiry, the redirect path, and the cache
- [ ] Accurate to `internal/links` + `internal/cache`

## Relation

- Umbrella: [#0062](0062.md). UTM passthrough on redirect is detailed in [#0070](0070.md).
