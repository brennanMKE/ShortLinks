# 0073 — Document real-time updates / SSE events (docs/events.md)

| | |
|---|---|
| **Status** | resolved |
| **Module** | events |
| **First seen** | 2026-06-22 |
| **Closed** | 2026-06-23 |
| **Commit** | 30d7e36 |

## Description

Write `docs/events.md` documenting the Server-Sent Events (SSE) system that drives live updates in the SPA.

## Scope

- The broker design (`internal/events`): subscribers, publish/fan-out, lifecycle.
- The `/api/events` endpoint and the Apache `flushpackets=on` proxy requirement (so events flush immediately) — cross-link `DEPLOYMENT.md` Apache section.
- What events are published and when.
- How the SPA subscribes and reacts (`web/src/lib/events.ts`).

## Acceptance criteria

- [ ] `docs/events.md` covers the broker, the endpoint (incl. the Apache flush requirement), event types, and SPA consumption
- [ ] Accurate to `internal/events` + `web/src/lib/events.ts`

## Relation

- Umbrella: [#0062](0062.md).

## Resolution

Written in [`docs/events.md`](../docs/events.md) as part of the #0062 documentation set (commit `30d7e36`), grounded in the cited source files. First pass — review in the Mac app and flag any inaccuracies for a follow-up edit.
