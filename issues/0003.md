# 0003 — Apache 2 virtual host configuration for go.sstools.co

| | |
|---|---|
| **Status** | closed |
| **Module** | infra |
| **Platform** | All |
| **First seen** | 2026-05-25 |
| **Closed** | 2026-05-25 |
| **Commit** | 962421d |

## Description

Create the Apache 2 virtual host configuration file that proxies all traffic for `go.sstools.co` to the Go service on `localhost:8080`. The SSE endpoint requires special treatment to disable response buffering.

## Acceptance Criteria

- [ ] Config file created at `deploy/apache/go.sstools.co.conf`
- [ ] Port 443 virtual host with `ProxyPreserveHost On`
- [ ] `/api/events` proxied with `flushpackets=on` (listed before the wildcard `ProxyPass`)
- [ ] All other traffic proxied to `http://127.0.0.1:8080/`
- [ ] SSL certificate paths reference Let's Encrypt paths for `go.sstools.co`
- [ ] `deploy/apache/README.md` documents:
  - Required Apache modules (`a2enmod proxy proxy_http ssl`)
  - How to install: `sudo cp go.sstools.co.conf /etc/apache2/sites-available/ && sudo a2ensite go.sstools.co && sudo systemctl reload apache2`
  - How to obtain the Let's Encrypt certificate (`certbot --apache -d go.sstools.co`)

## Notes

PRD reference: Apache Configuration section.
The `flushpackets=on` directive on the SSE route must appear before the wildcard `ProxyPass /` line, otherwise Apache matches the wildcard first and buffering is not disabled.

## Implementation

Created `deploy/apache/go.sstools.co.conf`, a port-443 `<VirtualHost *:443>` for
`go.sstools.co` matching the PRD's Apache Configuration block:

- `ProxyPreserveHost On`.
- `/api/events` proxied to `http://127.0.0.1:8080/api/events` with
  `flushpackets=on`, with its `ProxyPass`/`ProxyPassReverse` pair listed
  **before** the wildcard, so SSE buffering is disabled.
- Wildcard `ProxyPass / http://127.0.0.1:8080/` plus matching `ProxyPassReverse`.
- `SSLEngine on` with Let's Encrypt cert paths for `go.sstools.co`
  (`fullchain.pem` / `privkey.pem`).

Also added `deploy/apache/README.md` documenting required modules
(`a2enmod proxy proxy_http ssl`), the install/enable/reload steps, and certbot
provisioning via `certbot --apache -d go.sstools.co`.

## Verification

- Verified by inspection: events `ProxyPass` (line 7) precedes the wildcard
  (line 10); port is 443; cert paths are correct; `<VirtualHost>` tags balanced
  (1 open / 1 close).
- `go build ./...` succeeds — the Go build is unaffected.
- Apache config syntax could **not** be checked against a live `apachectl` /
  `httpd -t` in this environment (Apache is not installed), so syntax validation
  relied on inspection against the PRD's authoritative vhost block.

## Files changed

- `deploy/apache/go.sstools.co.conf` (new)
- `deploy/apache/README.md` (new)

## Gotchas

`ProxyPass` directives are evaluated top-to-bottom; the SSE route must precede
the wildcard or Apache matches `/` first and `flushpackets=on` never applies.
