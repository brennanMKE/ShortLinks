# 0117 — The short-link display base is hard-coded to `go.sstools.co` instead of coming from `BASE_URL`

| | |
|---|---|
| **Status** | resolved |
| **Module** | web / handlers / config |
| **Platform** | All |
| **First seen** | 2026-08-25 |
| **Closed** | 2026-08-25 |
| **Commit** | `801e7ef` |

## Description

Every short URL the product shows, copies, exports, or encodes into a QR code is built from a **hard-coded** `https://go.sstools.co`, not from the deployment's `BASE_URL`. There are two copies of the constant, one per language, and a comment on each telling the reader to keep them in sync by hand:

- `web/src/lib/links.ts:19` — `export const SHORT_URL_BASE = 'https://go.sstools.co';`
- `internal/qr/qr.go:59` — `const ShortURLBase = "https://go.sstools.co"`

`BASE_URL` already exists, is already required by `internal/config` (`config.go:117`), and is already documented in `DEPLOYMENT.md:153` as "Public base URL". It is simply not used for the one thing its name describes. Today it only feeds auth email links (`internal/auth/mailer.go`).

The consequence is that this codebase cannot be deployed under any other domain without editing Go and TypeScript source. `DEPLOYMENT.md:11` tells an operator to "substitute your domain wherever `go.sstools.co` appears and set `BASE_URL`" — the second half of that sentence does nothing for short links, and the first half is a source edit, not configuration.

## Why this matters

- **It is a source edit masquerading as configuration.** An operator who sets `BASE_URL` correctly still gets `go.sstools.co` on every copy button, CSV export, and QR code.
- **Two hand-synced constants across a language boundary will drift.** `qr.go`'s own comment says so: "Keep this in sync with links.ts's SHORT_URL_BASE by hand — there is no shared source of truth across the Go/TS boundary." A QR code that disagrees with the short URL printed next to it is a silent, on-paper failure.
- **Dev cannot exercise the real path.** `links.ts`'s comment argues the production base is deliberate because "a copied localhost link is useless to share". That is backwards: in dev `BASE_URL` *is* localhost, and a localhost link is exactly what works on the machine running the dev server. Today a dev-mode copy button hands you a URL that resolves to production.

## Scope / deliverables

- Serve the configured `BASE_URL` to the SPA rather than compiling a domain into it. `GET /api/me` is the natural carrier — the SPA already calls it on boot, and short URLs are only ever displayed to an authenticated user.
- Make `qr.ShortURL` take the base as an argument, sourced from `config.Config.BaseURL`, so the QR payload and the displayed URL are derived from one value.
- Normalize a trailing slash on `BASE_URL` so `https://x.co/` and `https://x.co` produce the same short URL.
- Delete both hard-coded constants and the "keep in sync by hand" comments they carry.
- Update `DEPLOYMENT.md` so the domain-substitution instruction reflects what is actually configurable.

## Acceptance criteria

- [x] No occurrence of `go.sstools.co` remains in `web/src/**` or `internal/**` outside of documentation and test fixtures.
- [x] With `BASE_URL=https://example.test`, the dashboard's copy button, `LinkDetail`'s share URL, the campaign CSV export's `ShortURL` column, and the QR payload all produce `https://example.test/u/{key}`.
- [x] `BASE_URL=https://example.test/` (trailing slash) produces the same result — no `//u/`.
- [x] `go build ./... && go test ./...` green (except the pre-existing [#0116](0116.md) `internal/config` failure); `cd web && npm run build` and `npm test` green.
- [x] `gofmt -l` clean for any file touched.

## Out of scope

- `cmd/shortlinks/seed.go`'s `seedLinkPrefix` — that is fixture data for seeded demo rows, and its doc comment states the fixed value is deliberate. Worth revisiting, not here.
- `WEBAUTHN_RP_ID` / `WEBAUTHN_RP_ORIGIN`, which are separately configured and already read from the environment.
- Multi-domain support (the PRD's "Custom domains" future item at `PRD.md:826`). This issue is one configured base, not N.

## Notes

- `internal/config` already requires `BASE_URL` unconditionally, so no new validation or fallback is needed — the value is guaranteed non-empty at startup.
- The SPA reads the base once at boot; `shortUrl()` must therefore resolve it at call time rather than capturing a module-load-time constant.

## Relation

- Discovered while diagnosing [#0118](0118.md) (the create-link error message), which is otherwise unrelated.

## Root cause

Nothing was broken — the base was never wired to configuration in the first
place. `BASE_URL` was added for auth email links and stayed scoped to that;
short links got a constant instead, once per language, each with a comment
instructing future readers to keep the two in sync manually. The comment on
`links.ts`'s constant argued the production value was *deliberate* ("a copied
localhost link is useless to share"), which is what kept it in place: the
hard-coding read as a decision rather than an omission. It is backwards — in
dev, `BASE_URL` is localhost, and a localhost link is the one that works on the
machine running the dev server.

## Fix

`BASE_URL` is now the single source for every externally visible short URL.

`GET /api/me` gained a `base_url` field, trailing-slash-trimmed by the server.
The SPA already calls that endpoint on boot in all four sign-in paths (App,
Login, RegisterVerify, RecoverVerify), and short URLs are only ever shown to an
authenticated user, so no new endpoint or round trip was needed. `shortUrl()`
reads it off the `currentUser` store at call time rather than capturing a
module-load constant — an untracked `get()` read, since `base_url` is fixed for
a session and arrives before any view that displays a short URL renders. The
fallback when no profile has landed yet is `location.origin`, the correct guess
for a same-origin SPA.

On the Go side `qr.ShortURL` takes the base as an argument, threaded from
`config.Config.BaseURL` through `NewLinksHandler`, `NewCampaignsHandler`, and
`buildCampaignExportRows`. Both hard-coded constants are gone, along with the
"keep in sync by hand" comments.

## Verification

- `TEST_DATABASE_URL=… go test ./...` — all packages pass except the
  pre-existing `internal/config` `TestLoad_DefaultsApplied` failure tracked as
  [#0116](0116.md), which is unrelated and predates this branch. `internal/qr`
  and `internal/handlers` (the two packages this touched) are green, including
  the DB-backed handler suites.
- New Go coverage: `TestShortURL_TrailingSlashOnBase` (qr) and
  `TestMe_BaseURLTrailingSlashTrimmed` (handlers).
- `cd web && npx tsc --noEmit` clean; `npm test` — 375 passed.
- **Ran the app** (`./scripts/dev.sh`, `BASE_URL=http://localhost:8080`) and
  drove it in a browser. `GET /api/me` returned
  `{"…","base_url":"http://localhost:8080"}`; the dashboard's Copy button put
  `http://localhost:8080/u/solder0903` on the clipboard; the link-detail page
  showed the same URL as its Short URL; `GET /api/links/{key}/qr.svg` returned
  200. Nothing displayed `go.sstools.co`.

## Files changed

- `internal/qr/qr.go` — `ShortURLBase` deleted; `ShortURL(base, key)` takes the
  base and trims a trailing slash.
- `internal/handlers/links.go` — `baseURL` field + constructor arg; QR endpoints
  build from it.
- `internal/handlers/campaigns.go` — same, for the bulk QR archive.
- `internal/handlers/campaigns_export.go` — `buildCampaignExportRows` takes the
  base for the CSV's short-URL column.
- `internal/handlers/me.go` — `base_url` on the `GET /api/me` response,
  trailing-slash-trimmed.
- `internal/config/config.go` — documented `BaseURL` as the single source for
  every externally visible URL.
- `cmd/shortlinks/main.go` — passes `cfg.BaseURL` in both the Postgres and dev
  wirings.
- `web/src/lib/links.ts` — `SHORT_URL_BASE` deleted; `shortUrlBase()` reads the
  profile store.
- `web/src/lib/types.ts` — `User.base_url`.
- `web/src/views/CampaignDetail.svelte` — assign-links placeholder built from
  the configured base.
- `web/src/lib/campaigns.ts` — doc comment no longer names a domain.
- `DEPLOYMENT.md`, `.env.example` — `BASE_URL` documented as what it now
  actually controls.
- Tests: `internal/qr/qr_test.go`, `internal/handlers/{qr,me,links,campaigns,campaigns_export,events,url_filters,audit_seams}_test.go`,
  new `internal/handlers/testbaseurl_test.go`, `web/src/lib/{links,campaigns}.test.ts`.

## Gotchas

- Tests deliberately use `https://example.test`, never the production domain. A
  regression that reintroduced a hard-coded `go.sstools.co` would otherwise pass
  by coincidence in exactly the tests meant to catch it.
- The SPA's fallback base is `location.origin`, so a missing `base_url` degrades
  to something plausible instead of an obviously broken URL — which means a
  future regression in the `/api/me` payload would be quiet. The `me` tests
  assert the field explicitly for that reason.
- `web/dist/index.html` is a committed placeholder; running `npm run build` for
  verification overwrites it and it must be restored before committing (per
  `Issues.md`).
- `scripts/dev.sh` frees port 8080 with `kill -9`. On this machine that port was
  held by Safari's WebDriver service (`safaridriver`), which the script killed.
